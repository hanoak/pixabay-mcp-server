# Required. Get a free key at https://pixabay.com/api/docs/ (sign up / log in, then
# your key is shown on that page — no separate approval step for the default tier).
PIXABAY_API_KEY=

# Optional. One of: debug, info, warn, error. Defaults to info.
LOG_LEVEL=
